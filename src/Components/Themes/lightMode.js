import { createTheme } from "@mui/material";

export const lightMode = createTheme({
        mode: "light",
        palette: {
            primary: {
                main: "#80cbc4" 
            },
            secondary: {
                main: "#b2dfdb"
            }
        }
})